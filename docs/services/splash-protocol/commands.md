# Command Flow

This repository-local page is no longer the canonical `splash-protocol`
command-flow document.

Canonical wiki page:
- `Services/Splash Protocol/Command Flow`

Current local rule:
- use the Gitea wiki as the primary source for protocol command-flow behavior
- treat this file only as a migration pointer during the wiki transition

Local guardrail reminder:
- equipment is the primary source of configuration truth
- every live controller config write must begin from a fresh live read
