# Splash API Runtime

This repository-local page is no longer the canonical Splash API runtime
document.

Canonical wiki page:
- `Services/Splash API/Runtime`

Current local rule:
- use the Gitea wiki as the primary source for Splash API runtime behavior
- treat this file only as a migration pointer during the wiki transition
