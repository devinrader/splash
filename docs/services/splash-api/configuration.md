# Splash API Configuration

This repository-local page is no longer the canonical Splash API configuration
document.

Canonical wiki page:
- `Services/Splash API/Configuration`

Current local rule:
- use the Gitea wiki as the primary source for Splash API configuration design
- treat this file only as a migration pointer during the wiki transition
