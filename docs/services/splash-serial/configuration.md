# Splash Serial Configuration

This repository-local page is no longer the canonical Splash Serial
configuration document.

Canonical wiki page:
- `Services/Splash Serial/Configuration`

Current local rule:
- use the Gitea wiki as the primary source for Splash Serial configuration
  design
- treat this file only as a migration pointer during the wiki transition
