# Workflows

This repository-local page is no longer the canonical workflows document.

Canonical wiki page:
- `Workflows/Platform Workflows`

Current local rule:
- use the Gitea wiki as the primary source for workflow design
- treat this file only as a migration pointer during the wiki transition
