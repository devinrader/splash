# REST API

This repository-local page is no longer the canonical REST API specification.

Canonical wiki page:
- `Interfaces/REST API`

Current local rule:
- use the Gitea wiki as the primary source for REST API behavior
- treat this file only as a migration pointer during the wiki transition
