# SSE Events

This repository-local page is no longer the canonical SSE-events document.

Canonical wiki page:
- `Interfaces/SSE Events`

Current local rule:
- use the Gitea wiki as the primary source for event-stream interface design
- treat this file only as a migration pointer during the wiki transition
