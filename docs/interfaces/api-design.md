# API Design

This repository-local page is no longer the canonical API-design document.

Canonical wiki page:
- `Interfaces/API Design`

Current local rule:
- use the Gitea wiki as the primary source for interface design
- treat this file only as a migration pointer during the wiki transition
